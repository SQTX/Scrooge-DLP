# ============================================================================
# Rust / Cargo
# ============================================================================
target/
**/target/
Cargo.lock.bak
*.rs.bk

# Generated by cargo when building
**/*.rs.bk

# MSVC Windows builds of rustc generate these
*.pdb

# Coverage reports
*.profraw
*.profdata
tarpaulin-report.html
cobertura.xml
lcov.info

# Rust-analyzer / IDE caches
.rust-analyzer/

# ============================================================================
# Protobuf - generated code (jeśli generujesz do plików zamiast OUT_DIR)
# ============================================================================
# Domyślnie tonic-build generuje do OUT_DIR więc to nie jest potrzebne,
# ale na wszelki wypadek:
crates/scrooge-proto/src/generated/
*.pb.rs

# ============================================================================
# Konfiguracja lokalna i sekrety
# ============================================================================
.env
.env.local
.env.*.local
*.env

# Lokalne overrides konfiguracji
config.local.yaml
config.local.toml
manager.local.yaml
agent.local.yaml
**/local.yaml

# ============================================================================
# Certyfikaty i klucze - NIGDY do gita
# ============================================================================
*.pem
*.key
*.crt
*.cer
*.der
*.p12
*.pfx
*.jks
*.keystore

# Katalog z certami (zarówno dev jak i prod)
certs/
**/certs/
ca/
**/ca/
pki/
**/pki/

# Tokens enrollment, JWT secrets
*.token
enrollment-token*
jwt-secret*

# SSH keys (jeśli ktoś przypadkiem wrzuci)
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
*.ppk

# ============================================================================
# Bazy danych
# ============================================================================
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite3
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# Dane lokalne agenta i managera
data/
**/data/
agent-data/
manager-data/

# PostgreSQL volume mounts (jeśli ktoś zmieni docker-compose)
postgres-data/
pg-data/
pgdata/

# ============================================================================
# Logi
# ============================================================================
*.log
logs/
**/logs/
log/
**/log/

# Tracing/journalctl exports
*.jsonl
trace-*.json

# ============================================================================
# Docker
# ============================================================================
# Volumes lokalne
.docker/
docker-volumes/

# Docker compose overrides (lokalne)
docker-compose.override.yml
docker-compose.override.yaml
docker-compose.local.yml
docker-compose.local.yaml

# ============================================================================
# Quarantine i runtime files agenta
# ============================================================================
quarantine/
**/quarantine/
runtime/
**/runtime/
*.pid
*.sock

# ============================================================================
# macOS
# ============================================================================
.DS_Store
.AppleDouble
.LSOverride
Icon
._*
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Files that might appear in the root of a volume
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# ============================================================================
# Linux
# ============================================================================
*~
.fuse_hidden*
.directory
.Trash-*
.nfs*

# ============================================================================
# Windows (na przyszłość, gdy dojdzie scrooge-agent-windows)
# ============================================================================
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
*.stackdump
[Dd]esktop.ini
$RECYCLE.BIN/
*.cab
*.msi
*.msix
*.msm
*.msp
*.lnk

# ============================================================================
# IDE / Edytory
# ============================================================================

# VSCode
.vscode/
!.vscode/settings.json.example
!.vscode/extensions.json.example
*.code-workspace

# IntelliJ / RustRover / CLion
.idea/
*.iml
*.iws
*.ipr
out/

# Vim
*.swp
*.swo
*.swn
.*.swp
Session.vim
.netrwhist

# Emacs
*~
\#*\#
.\#*
.emacs.desktop
.emacs.desktop.lock
*.elc
auto-save-list
tramp

# Sublime Text
*.sublime-workspace
*.sublime-project

# Zed
.zed/

# Helix
.helix/

# ============================================================================
# Claude Code / AI tooling
# ============================================================================
.claude/
.aider*
.cursor/

# ============================================================================
# Temp files
# ============================================================================
*.tmp
*.temp
*.bak
*.backup
*.orig
tmp/
temp/
scratch/

# ============================================================================
# Test artifacts
# ============================================================================
test-results/
test-output/
.pytest_cache/
.coverage

# Integration test fixtures z runtime data
tests/fixtures/runtime/
tests/tmp/

# ============================================================================
# Documentation builds
# ============================================================================
target/doc/
book/
docs/_site/
docs/.jekyll-cache/

# ============================================================================
# Profiling / Performance
# ============================================================================
flamegraph.svg
perf.data
perf.data.old
*.perf
callgrind.out.*
massif.out.*
cachegrind.out.*

# ============================================================================
# Dla CI/CD
# ============================================================================
# GitHub Actions może cachować lokalnie przy testach `act`
.act/

# ============================================================================
# Frontend (na przyszłość, gdyby dashboard trafił do tego repo)
# ============================================================================
node_modules/
dist/
.next/
.nuxt/
.svelte-kit/

# ============================================================================
# Specyficzne dla ScroogeDLP
# ============================================================================

# Lokalne kopie polityk (development)
policies/local/
policies/*.local.yaml

# Generated OpenAPI spec (jeśli zapisywany do pliku)
openapi.local.json

# Wazuh test fixtures z prawdziwymi logami
deploy/wazuh-integration/test-data/

# Lokalne enrollment tokens (development)
.enrollment-tokens

# Agent persistent state
agent-state/
**/agent-state/

# Manager state (poza Dockerem)
manager-state/

# Dev VM artifacts
deploy/dev/vm-snapshots/
deploy/dev/*.iso
deploy/dev/*.qcow2
deploy/dev/*.utm/

# ============================================================================
# Pliki, które JAWNIE chcemy zachować (negacje)
# ============================================================================
# Te negacje są ważne - chronią pliki które mogłyby zostać złapane wzorcem wyżej

# Przykładowe konfigi i polityki (te chcemy w gitcie!)
!policies/default.yaml
!policies/finance-strict.yaml
!policies/developer-friendly.yaml
!policies/examples/
!**/example.env
!**/.env.example

# Test certyfikaty dla testów integracyjnych (oznaczone jako test)
!tests/fixtures/test-ca.pem
!tests/fixtures/test-cert.pem
!tests/fixtures/test-key.pem
# ── ScroogeDLP dev artifacts (wygenerowane przez make dev-*) ────────────────
dev-certs/
agent-data/
*.cert
agent.id
